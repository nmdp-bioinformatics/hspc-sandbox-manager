server:
  port: 8080
  session:
    cookie:
      name: SBM_JSESSIONID
  tomcat:
    remote_ip_header: x-forwarded-for
    protocol_header: x-forwarded-proto

spring:
  application:
    name: HSPC Sandbox Manager

multipart:
  enabled: true
